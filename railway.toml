[build]
builder = "nixpacks"
dockerfilePath = ""

[deploy]
startCommand = "python manage.py migrate && python manage.py collectstatic --noinput && gunicorn core.wsgi --bind 0.0.0.0:$PORT --timeout 120 --workers 1"
restartPolicyType = "on_failure"